#cloud-config
package_update: true
package_upgrade: true

packages:
  - nginx

runcmd:
  - systemctl enable nginx
  - echo "<h1>This is Az 700 DDOS LAB $(hostname)</h1>" > /var/www/html/index.html
  - systemctl start nginx
  